<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.apps.afis.crismasantuario">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning,InnerclassSeparator">
        <activity
            android:name=".Main.MainActivity"
            android:label="@string/place_name" />
        <activity
            android:name=".Main.SplashScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Cadastro.CadastroCatequistaActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Administrador.GerenciaCrismandosActivity" />
        <!--<activity android:name=".Administrador.Supervisor.GerenciaTurmasActivity" />-->
        <activity android:name=".Administrador.Supervisor.AdicionaTurmasActivity" />
        <activity android:name=".Administrador.AdicionarCrismandosActivity"/>
    </application>

</manifest>